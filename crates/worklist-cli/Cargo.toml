[package]
name = "worklist-cli"
version = "0.1.0"
edition = "2021"

license.workspace = true

[[bin]]
name = "worklist"
path = "src/main.rs"

[dependencies]
worklist = { path = "../worklist" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
