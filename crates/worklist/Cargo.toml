[package]
name = "worklist"
version = "0.1.0"
edition = "2021"

license.workspace = true

[dependencies]
regex = "1"
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
atlassian-markdown-converter = "0.1.0"

[dev-dependencies]
tempfile = "3"
