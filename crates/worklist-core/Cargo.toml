[package]
name = "worklist-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
regex = "1"
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
atlassian-markdown-converter = "0.1.0"

[dev-dependencies]
tempfile = "3"
