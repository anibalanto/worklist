[package]
name = "worklist-server"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "worklist-server"
path = "src/main.rs"

[dependencies]
worklist-core = { path = "../worklist-core" }
worklist-provider = { path = "../worklist-provider" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
